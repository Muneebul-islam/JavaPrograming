public class Relational_operators {
  // The code snippet is a Java program that compares two integer variables `x` and `y` using the
  // equality operator `==`. If `x` is equal to `y`, it will print "equal" to the console. In this
  // case, since `x` is 45 and `y` is 5, they are not equal, so the message "equal" will not be
  // printed.
    public static void main(String[] args) {
        int x = 45;
        int y = 5;
        if(x==y){
            System.out.println("equal");
        }
    }
}
